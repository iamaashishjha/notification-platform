# Notification Admin UI

React, Vite, TypeScript, and Tailwind dashboard for platform admins and tenant users.

```sh
npm install
npm run dev
```

The UI reads `VITE_API_BASE_URL`; in Docker Compose it points to `http://localhost:8080`.
